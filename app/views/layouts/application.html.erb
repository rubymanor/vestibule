<!DOCTYPE html>
<html>
<head>
  <title><%= render_page_title %></title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<div id="header">
  <div id="account_nav">
    <% if account_signed_in? -%>
      <%= render 'shared/account', :account => current_account %>
      <span><%= link_to "Sign out", destroy_account_session_path %></span>
    <% else -%>
      <span><%= link_to "Sign up", new_account_registration_path %></span>
      <span><%= link_to "Sign in", new_account_session_path %></span>
    <% end -%>
  </div>
</div>

<%= render :partial => "shared/notices" %>

<% if remind_account_for_signup_reason -%>
  <p>You haven't provided a reason on why you signed up yet.</p>
  <p><%= link_to "Provide one now", edit_user_path %></p>
<% end -%>

<%= yield %>

</body>
</html>
